spring:
  datasource:
    url: jdbc:postgresql://order-db:5432/postgres
    username: eden
    password: paroxx645213

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show-sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
